# Function: mightFailFunction()

```ts
function mightFailFunction<T>(promise: Promise<T>): Promise<Either<T>>
```

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `promise` | `Promise`\<`T`\> |

## Returns

`Promise`\<[`Either`](/type-aliases/Either.mdx)\<`T`\>\>
